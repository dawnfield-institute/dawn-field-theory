# Runtime dependencies for experiments and repository tooling.
#
# PyTorch is deliberately absent: platform wheel variants (CPU / CUDA versions) cannot be
# expressed portably here. Install it per your platform if you need reality-engine or the
# GAIA experiments — https://pytorch.org/get-started/locally/
#
# Versions are floors, not pins. Pin exactly in a lock file when snapshotting a result
# for a preprint (see ENVIRONMENT.md).

# --- experiments ---
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
sympy>=1.12
pandas>=2.0
networkx>=3.0
scikit-learn>=1.3
# FITS catalogue I/O -- required by the Midnight sidecar's SDSS scripts (exp_05-08,
# exp_18, exp_19, exp_23). Was missing, so those scripts could not run on a
# spec-compliant install.
astropy>=5.3

# --- repository tooling (tools/, and the CI gate) ---
PyYAML>=6.0
