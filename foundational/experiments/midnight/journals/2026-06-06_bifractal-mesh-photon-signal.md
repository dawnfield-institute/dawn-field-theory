# The Bifractal Mesh: Photons as Fragments of a Collective Signal

**Date:** 2026-06-06
**Author:** Peter Groom + Claude
**Status:** Active — testing against SDSS data

---

## The Insight

After running exp_03 (alpha invariance, 4/4) and exp_04 (cascade width oscillation, 4/4), we probed real SDSS DR16 MgII absorber data (159,524 systems). Individual line widths show ~18% non-monotonic variation with redshift, with partial alignment to cascade transition redshifts (N=6 peak at z=1.416 matches a local maximum; N=5.5 trough region shows the lowest FWHM).

But the model predicted 400x contrast and we see 18%. Peter's insight: **a photon is a piece of a larger puzzle of other photons.** The individual measurement is incomplete by design — a photon is a ledger severance event (M-R), a fragment CUT from a larger PAC structure. The full signal isn't in any single photon's width. It's in how photons relate to each other — the bifractal mesh.

## The Connection to DFT

1. **Ledger severance (M-R):** Radiation = cutting a piece of the PAC ledger. The photon carries the severed fragment's properties. But the ledger it was cut FROM has structure that the fragment alone cannot reconstruct.

2. **Coherence as overlooked channel (phase-rate journal Part I.2):** We use photons as "light bulbs" (energy, spectrum, width) — individual properties. The overlooked channel is coherence — how photons relate to each other. "Computing, not light bulb."

3. **Bifractal convergence (Feigenbaum strengthening):** The PAC recursion produces a bifractal structure — two interleaved fractal scales converging to Xi = gamma + ln(phi). Photons from a given epoch carry positions in this bifractal mesh.

## The Refined Prediction

**Individual photon widths** encode LOCAL disequilibrium — one node's state in the mesh. That's the ~18% modulation we observe.

**Photon-photon correlations** encode MESH structure — how nodes relate across the bifractal. At cascade transitions (integer N), SEC is actively restructuring the mesh. This should produce:

- **Enhanced FWHM variance** at transition redshifts (diverse states during restructuring)
- **Reduced FWHM variance** at settled redshifts (uniform states between transitions)
- **Different EW distributions** at transitions vs troughs (skewness, kurtosis, strong/weak ratio)
- **Width-width correlations** between absorbers at similar z that oscillate with N(z)

The full cascade signal lives in the COLLECTIVE statistics, not the individual measurements.

## Testable Now

We have 160K absorbers with FWHM, EW, z, and sky positions. The variance analysis is immediate: bin by z, compute FWHM variance per bin, check if variance oscillates with the cascade clock.

## What This Means for Midnight

This is Thread 1 (Photon Archaeology) connecting to the bifractal mesh — possibly a new thread or a deepening of Thread 1 into collective/correlation territory. The single-photon prediction was the first layer. The mesh prediction is the second layer. If the variance analysis shows cascade-correlated structure, we're looking at the mesh poking through.
