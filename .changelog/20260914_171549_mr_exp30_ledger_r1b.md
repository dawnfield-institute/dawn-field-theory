# Milestone R exp_30 — R1b on fresh seeds: 3/4, the kill fires; the 2.8 σ was seed selection, the ledger is a bound-maker, and the web goes where the pressure's net work turns positive

**Date**: 2026-09-14 · **Branch**: `exp/mr-30-ledger-r1b` (off main) · **Registration sealed**: `a4ab7a68`

## What

exp_29's unscored post-hoc number — at n = 4000 the ledgered engine at κ = 1 holds more web than
gravity alone in every seed, 2.8 σ — tested as a registered claim on seeds it had never seen
({4, 5, 6}), full size only, with the box-to-range ratio declared as a condition of the claim
(box / 2r₀ = 3.0 ≥ 3; the proxy at 1.89 retired as a decider for pressure-range questions), a new
arm κ = 1.5 whose position was predicted before it was run, and the same instruments and ledger
as exp_29 (reality-engine `feat/v4-pac-ledger-r1b`, one harness line so the runs land in
`results/full_r1b/`). M = 4, invariants only; the scorer refuses seeds 1–3 and any incomplete grid.

## Result

**3/4, and the registered kill fires.** T1 FAIL: κ = 1 holds 0.851 / 0.572 / 0.793 against gravity
alone 0.645 / 0.631 / 0.684 — 2/3 seedwise, margin 0.085, pooled σ 0.106, 0.8 σ. The κ = 1 arm
reproduced its mean (0.74 vs 0.75); the control moved — gravity alone is high and tight on the
fresh seeds (0.65 ± 0.03) where it was low and spread on seeds 1–3 (0.44 ± 0.15). The 2.8 σ was
gravity's two low seeds. T2 PASS: bound at κ = 1 (max KE/|U| 0.34–0.38) and at κ = 1.5 (0.46–0.47).
T3 PASS: κ = 1.5 landed where the virial arithmetic put it, 3/3 on both clauses (KE/|U| 1 < 1.5 < ∞;
percolation 1 > 1.5). T4 PASS: exact, within allowance, the budget binds. Scorecard 65/120 → 68/124.

**Reading.** Retired: the mapping as a structure-adder beyond gravity. Kept: the ledger as the
bound-maker (KE/|U| ≈ ⅓ at κ = 1, the pressure paying for itself). New, descriptive: the web goes
exactly where the bounded pressure's net work turns positive — negative at κ ≤ 1 (−0.8 to −4.3 P₀),
positive at κ ≥ 1.5 (+0.2 to +0.3 P₀), percolation 0.57–0.85 on one side and 0.09–0.39 on the other.
Exploring, unscored: over six seeds the ledgered arms sit above gravity in 11/12 pairs at 1.0–1.6 σ,
and the best-looking arm moved from κ = 1 to κ = 0.5 between seed sets — seed noise with the arm
chosen after the fact; not chased. Next: **R2**, severance on the bound substrate at κ = 1 (κ = 0.5
for margin is Peter's call at registration); the edge is a registrable side-claim there.

## Files

- `experiments/sidecars/milestone-r/journals/2026-09-14_exp30_registration.md` (sealed),
  `2026-09-14_exp30_outcomes.md`; `scripts/exp_30_ledger_r1b.py`; `results/` grid copy (per-run
  SHA256s) and the scored JSON
- README (row 30, finding 43, one honest-failure row, P40; 68/124), meta.yaml, THEORY_MAP sidecar
  row, ROADMAP Milestone R section; generated indexes

## Process

Nothing ran before the seal; the scorer was exercised on synthetic grids and on exp_29's grid
(refused) beforehand. The scorer's first draft used the wrong grid schema and a paired-difference σ
and was corrected against exp_29's scorer before sealing. SP5 was mis-registered (the unbounded
engine sits below the random floor, as exp_29 already showed) and is recorded as a registration
error. No threshold moved after any result. Companion: reality-engine POC-12 `results/full_r1b/`
(`feat/v4-pac-ledger-r1b`, stacked on PR #11).
