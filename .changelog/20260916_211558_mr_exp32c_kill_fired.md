# Milestone R exp_32c — 2/4, the kill fired: kappa_c is not predictable from the initial large-scale power

Sealed ecfa0f6b with six predictions frozen before any run; 42 runs, zero gate failures.

- **T1 FAIL, skill -2.548.** The frozen line's RMS error is 0.044 against the null's 0.023 — predicting
  one fixed number beats it by nearly two. r(f, kappa_c) on the six FRESH seeds is **-0.064**; the
  +0.882 on the training six was a six-point accident. T3 FAIL 2/6. T2 PASS at +0.257 and is worth
  nothing: its registered null rate is 0.499.
- **T4 PASS 6/6 — twelve seeds out of twelve.** The edge is a single crossing.
- **The edge is now measured on twelve seeds: kappa_c = 1.1759 +/- 0.0073 (SE), sd 0.0251.** The SE
  falls 39% from exp_32b's six-seed figure. SP1 held.
- **The failure was predicted four minutes before it ran**, from data already on disk: f correlates
  with the endpoint and with none of its proximate causes (r with the reservoir -0.029, with the work
  -0.004, with the slope +0.101). That map is committed beside the outcomes as EXPLORING.
- **The kill sentence was mis-written** — it named a halt rather than a next move, violating the
  STANDARDS 2.7.9 added today. The seal stands; the outcomes carry the correction and the real next
  move: T/|U0| (r = +0.616 on twelve) then the Zel'dovich deformation tensor, neither registered until
  mapped.

Nothing here touches exp_32b's 4/4. What is retired is this statistic as a predictor, not the
question.
