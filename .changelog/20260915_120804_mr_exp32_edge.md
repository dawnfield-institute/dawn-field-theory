# Milestone R exp_32 — the edge as a number: UNSCORED by its own instrument gate; unofficially a single sign change on every fresh seed at κ_c ≈ 1.17, gravity-invariant 12/12; sec_balance cancels exactly (spec R4′)

**Date**: 2026-09-15 · **Branch**: `exp/mr-32-edge` (stacked on `exp/mr-31-paired-ledger`) · **Registration sealed**: `091ddab2`

## What

The sign change of the bounded pressure's net work between κ = 1 and 1.25 (exp_30/31, twelve seeds),
put in the ledger's arithmetic (W_p = T − ΔE_SEC; the compression ratio crossing 1) with a scaling
argument for its invariances, registered on fresh seeds 13–15: a fine κ sweep 1.00–1.25 in steps of
0.05, gravity arms g ∈ {0.75, 3.0}, a size arm n = 8000 (36 runs). The pair coupling was struck before
the seal — at half Ξ/φ the run reproduces the baseline bit for bit, because every growth is
budget-clipped from its first tick; recorded as reality-engine spec R4′.

## Result

**UNSCORED.** The sealed instrument gate (ledger identity within 0.02 |U₀|) fires on 29/36 runs: the
residual is the integrator's known truncation (0.02–0.05 |U₀|), documented in the scoping note at
~0.027 |U₀|; the tolerance was set below the instrument's floor — a registration error. Read
unofficially with the gate relaxed in a scratch copy: T1 would fail on its spread bar (a single
crossing on every seed, κ_c = 1.20 / 1.13 / 1.19 by interpolation, bracket spread 0.10 against a bar of
0.05 registered with no prior); T2 2/3 (the median crosses with the total on two seeds, trails by a
step on the third); **T3 12/12** (the edge does not move across a fourfold range of g); T4 2/3 (one seed
at n = 8000 has its edge at or just above 1.25). A second sealed-scorer defect (a float index in T2)
was found and fixed as a bug, criteria untouched.

## Files

- `journals/2026-09-15_exp32_registration.md` (sealed), `2026-09-15_exp32_outcomes.md`;
  `scripts/exp_32_edge.py`; `results/exp_32_edge_grid_{fine,g,double}_*.json`
- README row 32 (UNSCORED; scorecard unchanged at 71/128), meta.yaml, THEORY_MAP, ROADMAP
- Companion: reality-engine `feat/v4-ledger-virial` — instrumentation `a2dde18`/`c83ab8c`/`2b8c1d0`,
  results `bfba094`/`7be57cb`; PR #14

## Process

Three registration errors of one class in three rounds (exp_31 T2's saturating sign count; exp_32's
gate below the instrument floor and a spread bar with no prior). Rule recorded: measure what the
instrument resolves before setting any bar on it, and exercise the scorer on the real aggregator's
output on every code path before the seal. The R2 draft (`internal/dft/2026-09-15_exp33_r2_registration_DRAFT.md`, not in the repo until sealed)
is not sealed: its design pass showed whole-particle severance of the local-edge set cascades and
amputates (reality-engine POC-13, PR #15).
