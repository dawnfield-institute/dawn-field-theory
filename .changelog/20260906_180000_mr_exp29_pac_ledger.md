# Milestone R exp_29 — the ledger on particles: 3/4 at both sizes; the ledger holds the web, and whose web it is depends on the size

**Date**: 2026-09-06 · **Branch**: `exp/mr-29-pac-ledger` (stacked on `exp/mr-28-dynamical-severance`, PR #184) ·
**Registration sealed**: `43e4ebc9`

## What

exp_28's zero located and answered in the house's terms: SEC is local and free to inject, PAC is
global and needs a ledger, and the v4 substrate had none — its SEC pressure is already the gradient
of a pair energy at fixed entropy, and the whole non-conservation is the entropy ratchet (pair
energy created by entropy change 689,700 against pressure work 667,300 on the baseline).
Reality-engine POC-12 adds the ledger: a per-particle potential budget `P(0) = κ|U_grav(0)|m_i/Σm`
paying for entropy growth at the price the pair energy sets, decay repaying, the total
`KE + U + E_SEC + ΣP` conserved to truncation with the transfer exact to 10⁻⁶. Registered before
running (STANDARDS §2.7): M = 4, invariants only, two controls (gravity alone at κ = 0; the
unbounded engine at κ = ∞), the κ sweep {0.5, 1, 2}, a virial prediction stated from the
arithmetic, three pre-seal clause changes disclosed with reasons.

## Result

**3/4, and the registered kill fires.** T2 PASS: bound, KE/|U_grav| 0.49–0.53 at κ = 0.5 and
0.29–0.35 at κ = 1 at every window mark (10.7–18.3 unbounded). T3 PASS: the ordering above the
threshold, κ = 1 < 2 < ∞, 3/3. T4 PASS: net creation ≤ P(0) exactly; the budget binds on every
would-grow particle. **T1 FAIL at both sizes**: whole-set percolation at κ = 0.5 is 0.51–0.76 (proxy) and
0.44–0.78 (n = 4000), 4.8–5.7 σ above the unbounded engine, but against gravity alone the arm wins
2/3 on the proxy (0.2 σ) and 3/3 at n = 4000 by 1.1 σ against a 2 σ bar. The mapping is retired
as the object that holds structure beyond gravity at κ = 0.5 with the proxy deciding; the ledger
discipline stays as a gated instrument. Scorecard 62/116 → 65/120.

**Reading.** The ledger is right, and the two sizes disagree about the pressure: on the proxy
(pressure range 2r0 = 20, more than half its box) the bounded engine never adds to gravity's
web; at n = 4000 it does — κ = 1 holds 0.70–0.80, above gravity in every seed by 2.8 σ (post hoc,
unregistered), and κ = 2 collapses. A bounded density-sourced repulsion is not structurally
inert on a box large compared with its range. Next, registrable: **R1b** — κ = 1 at n = 4000 on
fresh seeds with the box-to-range ratio declared; then R2 on a substrate that is now actually
bound; the pressure's form stays a candidate behind R1b.

## Files

- `experiments/sidecars/milestone-r/journals/2026-09-06_exp29_registration.md` (sealed),
  `2026-09-06_exp29_outcomes.md`; `scripts/exp_29_pac_ledger.py` (thresholds byte-equal,
  `--selftest`, accepts both grids, passes only at every size present); `results/` grid copies with
  per-run SHA256s and the scored JSONs
- README (row 29, finding 42, one honest-failure row, P39; 65/120), meta.yaml, THEORY_MAP sidecar
  row, ROADMAP Milestone R section

## Process

The proxy decides, n = 4000 confirms. Three registration clauses moved before the seal on
calibration numbers, each with its reason in the registration's §0 (a tautological total-energy
clause; an ordering registered only where predicted; a per-particle binding statistic). No
threshold moved after any result. Companion: reality-engine POC-12 (`feat/v4-pac-ledger`).
